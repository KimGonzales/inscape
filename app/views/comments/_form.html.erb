<%= simple_form_for ([@photo, @photo.comments.build]) do |f| %>
  <p>
    <%= f.input :content, label: 'Your Comment', :class => 'form-control' %>
  </p>
  <br>
  
  <p>
    <%= f.submit :class => 'btn btn-info'%>
  </p>

  <% end %>
