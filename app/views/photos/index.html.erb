<div class="col-md-6 col-md-offset-3">
  <div class="transitions-enabled masonry photos">
    <% @photos.each do |photo| %>
        <div class="box panel panel-default">
          <%= link_to (image_tag photo.image.url), photo %>
          <h2><%= link_to photo.title, photo %></h2>
          <p><%= time_ago_in_words(photo.created_at)%> ago</p>
        </div>
      <% end %>
  </div>
</div >

<!--

Each photo should be given a data-id of its photo.id. When that photo is clicked the show page will be rendered.

There will be a next and previous button on that photo's show page. Oncee the next button is clicked, the current view should change to 
show the next photo's data. 

Think of implementing a photo partial for this photo index to build out this functionality 

-->